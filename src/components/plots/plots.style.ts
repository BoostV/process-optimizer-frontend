import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles()(() => ({
  titleButton: {
    float: 'right',
  },
  titleIcon: {
    color: 'white',
  },
}))

export default useStyles
