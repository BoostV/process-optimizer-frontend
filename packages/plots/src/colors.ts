// Shared objective colors — quality and cost. Used directly (fully opaque) for
// the result-row tints, the Pareto uncertainty bands and the legend, so each
// objective reads consistently across the UI. Tweak these two values to
// restyle every objective surface at once.
export const qualityColor = '#e2e8ec'
export const costColor = '#ede8e2'
